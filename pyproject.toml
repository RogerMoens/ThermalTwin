[project]
name = "ThermalTwin"
version = "0.1.0"
description = "Indoor temperature prediction using Netatmo weather data."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"

dependencies = [
    "numpy",
    "pandas",
    "scikit-learn",
    "tensorflow",
    "matplotlib",
]
